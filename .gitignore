# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
*.runfabric*
# Test binaries
*.test
.claude/
# Output of go build / go install
*.out
.venv/
# Go workspace file
go.work
*.tar.zstd.bz2
*.tar.zstd.xz
*.tar.zstd.lzma
*.tar.zstd.lz
*.tar.zstd.zst
*.tar.zstd.zstd.gz
*.tar.zstd.zstd.bz2
*.tar.zstd.zstd.xz
go.work.sum
stubplugin*
!**/testdata/stubplugin/
!**/testdata/stubplugin/**
# Dependency directories (rare with Go modules but sometimes used)
vendor/

# Registry runtime data (local DB, uploads)
registry/data/

# Build directories (root only; engine/internal/runtime/build is source code)
bin/
/build/
dist/
/runfabric
/runfabricd
# Binary left in engine/ when building from engine dir (use 'make build' → bin/runfabric)
engine/runfabric
engine/runfabric.exe
test_out.txt

# SDK build artifacts (each sdk/* has its own .gitignore for local tooling)
sdk/node/bin/

# Coverage reports
coverage.out
*.coverprofile

# Go benchmark outputs
*.bench

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
Thumbs.db

# Logs
*.log

# Environment files
.env
.env.*
!.env.example
!**/.env.example

# Temporary files
tmp/
temp/

misc/
# Generated files (optional — depends on your workflow)
*.gen.go

# Node (root or workspace)
node_modules/
.turbo
.artifacts
.corepack-cache
.runfabric
.coverage*

my-service*

# Cloud-sync (iCloud/Dropbox) conflict copies, e.g. "foo 2.ts", "bar 3.tsx"
* [0-9].ts
* [0-9].tsx
* [0-9].js
* [0-9].jsx
* [0-9].mjs
* [0-9].cjs
* [0-9].json
* [0-9].go
* [0-9].md
* [0-9].yml
* [0-9].yaml
* [0-9].css
* [0-9].html
