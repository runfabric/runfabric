// Package aiflow provides workflow/graph compilation and DAG types for AI workflows (Phase 14).
// It is a dedicated package boundary importable by app and cli without pulling in CLI or app.
// Implementations (node registry, DAG compiler, expression resolution) will be added in Phase 14.
package aiflow
