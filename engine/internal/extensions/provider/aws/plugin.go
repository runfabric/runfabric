// Package aws implements the RunFabric provider plugin for AWS Lambda.
// Plugin entry: New() in provider.go returns the Provider implementation;
// register with extensions/providers.Registry as "aws" and "aws-lambda".
package aws
