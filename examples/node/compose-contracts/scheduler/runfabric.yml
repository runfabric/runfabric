service: compose-scheduler
provider:
  name: gcp-functions
  runtime: nodejs

env:
  WORKER_ENDPOINT_ENV: RUNFABRIC_OUTPUT_WORKER_AWS_LAMBDA_ENDPOINT

functions:
  - name: scheduler
    entry: src/index.ts
    triggers:
      - type: cron
        schedule: "*/10 * * * *"
