service: compose-worker
provider:
  name: aws-lambda
  runtime: nodejs

env:
  API_ENDPOINT_ENV: RUNFABRIC_OUTPUT_API_CLOUDFLARE_WORKERS_ENDPOINT

functions:
  - name: worker
    entry: src/index.ts
    triggers:
      - type: queue
        queue: compose-jobs
