service: handler-scenario-single
provider:
  name: aws-lambda
  runtime: nodejs

functions:
  - name: api
    entry: src/index.ts
    triggers:
      - type: http
        method: GET
        path: /hello
