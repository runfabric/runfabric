service: hello-http
provider:
  name: aws-lambda
  runtime: nodejs

providerOverrides:
  cloudflare:
    name: cloudflare-workers
    runtime: nodejs

functions:
  - name: api
    entry: src/index.ts
    triggers:
      - type: http
        method: GET
        path: /hello
