# Integrations

Integrations contain external integration adapters used by extensions.

For example, your diagram includes `integrations/mcp/` as a dedicated integration for MCP-based tooling.

Current codebase: MCP appears as a separate node package (see `protocol/mcp`, which we will later map into this integration folder).

