{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://runfabric.dev/schemas/secrets.schema.json",
  "title": "Secrets",
  "description": "Secret key to environment variable mapping. Used in functions.*.secrets in runfabric.yml.",
  "type": "object",
  "additionalProperties": { "type": "string" }
}
